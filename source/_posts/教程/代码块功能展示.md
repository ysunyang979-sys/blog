---
title: 代码块功能展示
date: 2025-12-08 23:40:00
tags: [教程, 功能, 代码]
categories: Site Features
keywords: [代码高亮, 复制按钮, 展开收起]
description: 本文展示了博客的代码块功能,包括复制按钮和自动展开收起功能。
---

# 代码块功能展示

本文演示博客的代码块高级功能。

## ✨ 功能特性

### 1. 复制按钮

将鼠标悬停在代码块上方,右上角会出现**复制**按钮。点击即可复制全部代码。

### 2. 自动展开/收起

当代码超过 300px 高度时,会自动折叠并在底部显示**展开代码**按钮。

### 3. 按钮固定位置

- **复制按钮**: 始终固定在代码块右上角,即使滚动也不会移动
- **展开按钮**: 始终固定在代码块底部

## 📝 示例:短代码

这是一段简短的代码,不会触发折叠功能:

```javascript
// Hello World 示例
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("World");
```

## 📦 示例:长代码(自动折叠)

下面是一段较长的 C# Unity 代码,会自动折叠。尝试:
1. 鼠标悬停查看**复制按钮**
2. 滚动代码时,复制按钮保持在右上角
3. 点击底部**展开代码**按钮
4. 展开后,按钮变为**收起代码**

```csharp
// Unity Editor 工具示例
using UnityEngine;
using UnityEditor;
using System.Collections.Generic;

namespace MyGameTools.Modeling
{
    public class PivotModifierTool : EditorWindow
    {
        private static bool IsEditMode = false;
        private static Tool LastTool = Tool.None;

        [MenuItem("Tools/Modeling/Toggle Pivot Edit Mode %#p", false, 1)]
        static void SetModeAltShiftA() { CurrentMode = AnchorKeyMode.AltShiftA; }

        [MenuItem("Tools/Modeling/Toggle Pivot Edit Mode %#p", true)]
        static bool ValidateModeAltShiftA()
        {
            Menu.SetChecked("Tools/UI/Anchor Settings/Use [Alt + Shift + A]", CurrentMode == AnchorKeyMode.AltShiftA);
            return true;
        }

        private static AnchorKeyMode currentMode;
        public static AnchorKeyMode CurrentMode
        {
            get { return (AnchorKeyMode)EditorPrefs.GetInt(PREF_KEY, (int)AnchorKeyMode.AltShiftA); }
            set { EditorPrefs.SetInt(PREF_KEY, (int)value); }
        }

        public enum AnchorKeyMode
        {
            AltShiftA, // 默认
            AltZ,
            AltA
        }

        static void ToggleEditMode()
        {
            IsEditMode = !IsEditMode;
            
            if (IsEditMode)
            {
                // 进入编辑模式
                LastTool = Tools.current;
                Tools.current = Tool.None;
                SceneView.duringSceneGui += OnSceneGUI;
                Debug.Log("✅ Pivot Edit Mode: ON");
            }
            else
            {
                // 退出编辑模式
                Tools.current = LastTool;
                SceneView.duringSceneGui -= OnSceneGUI;
                Debug.Log("❌ Pivot Edit Mode: OFF");
            }
        }

        static void OnSceneGUI(SceneView sceneView)
        {
            if (!IsEditMode) return;

            Event e = Event.current;
            
            // 处理快捷键
            if (e.type == EventType.KeyDown && e.keyCode == KeyCode.Escape)
            {
                ToggleEditMode();
                e.Use();
            }

            // 绘制自定义手柄
            if (Selection.activeGameObject != null)
            {
                Transform t = Selection.activeGameObject.transform;
                
                EditorGUI.BeginChangeCheck();
                Vector3 newPosition = Handles.PositionHandle(t.position, t.rotation);
                
                if (EditorGUI.EndChangeCheck())
                {
                    Undo.RecordObject(t, "Move Pivot");
                    t.position = newPosition;
                }
                
                // 绘制提示文本
                Handles.Label(t.position + Vector3.up * 2, 
                    "Pivot Edit Mode\nPress ESC to exit", 
                    EditorStyles.helpBox);
            }
        }
    }
}
```

## 🎯 Python 示例

这是一个 Python 数据处理脚本:

```python
import numpy as np
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score, classification_report

class DataProcessor:
    """数据处理和模型训练类"""
    
    def __init__(self, data_path):
        self.data = pd.read_csv(data_path)
        self.model = None
        
    def preprocess(self):
        """数据预处理"""
        # 处理缺失值
        self.data.fillna(self.data.mean(), inplace=True)
        
        # 特征工程
        self.data['feature_ratio'] = self.data['feature_a'] / (self.data['feature_b'] + 1)
        
        # 编码分类变量
        self.data = pd.get_dummies(self.data, columns=['category'])
        
        return self.data
    
    def train_model(self, test_size=0.2):
        """训练随机森林模型"""
        X = self.data.drop('target', axis=1)
        y = self.data['target']
        
        X_train, X_test, y_train, y_test = train_test_split(
            X, y, test_size=test_size, random_state=42
        )
        
        self.model = RandomForestClassifier(
            n_estimators=100,
            max_depth=10,
            random_state=42
        )
        
        self.model.fit(X_train, y_train)
        
        # 评估模型
        y_pred = self.model.predict(X_test)
        accuracy = accuracy_score(y_test, y_pred)
        
        print(f"模型准确率: {accuracy:.4f}")
        print("\n分类报告:")
        print(classification_report(y_test, y_pred))
        
        return accuracy

# 使用示例
if __name__ == "__main__":
    processor = DataProcessor("data.csv")
    processor.preprocess()
    accuracy = processor.train_model()
```

## 💡 使用建议

1. **代码高亮**: 支持多种编程语言的语法高亮
2. **一键复制**: 方便快速复制代码片段
3. **自动折叠**: 长代码自动折叠,节省页面空间
4. **流畅体验**: 按钮位置固定,操作更直观

Enjoy coding! 🚀
