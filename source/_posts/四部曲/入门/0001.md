---
title: 异常捕获
date: 2024-10-00 01:00:01
categories:
    - unity
    - 四部曲
    - 入门
---

# 异常捕获



>作用：可以避免当代码报错时造成程序卡死的情况
<!-- more -->   
```csharp
try
{
    Concle.WriteLine("异常捕获");
    string str = Console.ReadLine();
    int i = int.Parse(str);
}
catch
{
    Console.WriteLine("输入的有误"); //如果try中的代码有误则会执行这里的代码
}
finally
{
    Consle.WriteLine("执行完毕"); //无论对错都会执行这里的代码
}
```

>练习：用户输入一个数字，如果有误则提示用户输入错误

```csharp
  try
  {
      string str1 = Console.ReadLine();
      int i1 = int.Parse(str1);
      Console.WriteLine("输入正确");
  }
  catch
  {
      Console.WriteLine("请输入正确的数字");
  }

```
>练习2：输入名字和各科的成绩，如果输入不对报错
```csharp

    try
    {
        Console.WriteLine("请输入你的姓名");
        string m = Console.ReadLine();

        Console.WriteLine("请你输入数学成绩");
        string mat = Console.ReadLine();
        float math = float.Parse(mat);

        Console.WriteLine("请你输入语文成绩");
        string ch = Console.ReadLine();
        float chinese = float.Parse(ch);

        Console.WriteLine("请你输入语文成绩");
        string en = Console.ReadLine();
        float english = float.Parse(en);

    }
    catch
    { 
        Console.WriteLine("输入错误");
    }
    
```


